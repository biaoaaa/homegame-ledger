{
  "name": "homegame-poker-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "serve": "python3 -m http.server 8000"
  }
}
